#include<stdio.h>

int main(){
    // int i = 34;
    // int *ptr = &i;
    // printf("The of ptr is %u\n",ptr);
    // ptr = ptr + 2;
    // // ptr = ptr + 1;
    // // ptr = ptr - 1;
    // // ptr++;
    // // ptr++;
    // printf("The of ptr is %u\n",ptr);
    // char i = 'c';
    // char *ptr = &i;
    // printf("The of ptr is %u\n",ptr);
    // ptr = ptr + 1;
    // printf("The of ptr is %u\n",ptr);
    // return 0
    float i = 2.33;
    float *ptr = &i;
    printf("The of ptr is %u\n",ptr);
    ptr = ptr + 1;
    printf("The of ptr is %u\n",ptr);
    return 0;;
}